#ifndef DEBUG_LOG_H
#define DEBUG_LOG_H


#ifdef DEBUG
#define DLOG(file_, func_, line_, msg_)\
do {\
    fprintf (stderr, "In [%s, %s] at line %d:\n", (file_), (func_), (line_));\
    fprintf (stderr, "\t%s\n", (msg_));\
} while (false);
#else
#define DLOG(file_, func_, line_, msg_)


#endif
