#ifndef BIT_BOARD_H
#define BIT_BOARD_H


typedef struct bit_board_tag
{
    uint64 king[2];
    uint64 queen[2];
    uint64 rook[2];
    uint64 knight[2];
    uint64 bishop[2];
    uint64 pawn[2];

} bit_board_t;


#endif
